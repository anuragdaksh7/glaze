
services:
  postgres:
    image: postgres:17
    container_name: glaze_postgres_container
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: glaze
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_glaze:/var/lib/postgresql/data

  backend:
    build: .
    container_name: glaze_backend_container
    restart: always
    ports:
      - "8080:4000"
    depends_on:
      - postgres

volumes:
  postgres_data_glaze:

